#include <iostream>
#include <vector>

using namespace std;

class Matrix
{
public:
	Matrix(){
		vector<int> t[0];
		vector<vector<int> [0]> tmp[0];
		m = tmp;
	};
	// Matrix(int num_rows, int num_cols){
	// 	vector<int> t[num_cols];
	// 	vector<t> tmp[num_rows];
	// 	m = tmp;
	// };



	~Matrix();
	
private:
	// vector<int> row;
	vector<vector<int>> m;
};

int main(){

	return 0;
}